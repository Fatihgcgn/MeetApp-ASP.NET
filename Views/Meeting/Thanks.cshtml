@model UserInfo
@{
ViewBag.Title = "Başvuru Durumu";
}
<main class="text-center">
    <h1 class="h4">Başvuru Durumu</h1>

    @if(Model.WillAttend == true)
    {
    <p>Teşekkürler, @Model.Name sizinle birlikte Toplantıya @ViewBag.UserCount kişi katılıyor.</p>
    <a class="btn btn-sm btn-outline-primary" href="/meeting/list">Katılımcıları Listele</a>
    }else
    {
        <p>@Model.Name, toplantıya katılamadığınız için üzgünüz.. :(</p>
        <a class="btn btn-sm btn-outline-primary" href="/meeting/apply">Başvuru Formu</a>
    }

</main>